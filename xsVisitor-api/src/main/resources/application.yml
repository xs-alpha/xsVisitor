spring:
  profiles:
    active: dev

mybatis-plus:
  mapper-locations: classpath*:/mapping/*.xml
  typeAliasesPackage: com.xiaosheng.dao.po
  config-location: classpath:/mybatis-config.xml
  global-config:
    sql-parser-cache: true
    refresh: true
server:
  port: 8084